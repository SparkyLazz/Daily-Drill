# Tuesday, 2026-09-15 — Coding L2 / Logic L1

> **This problem has now gone 1 day unanswered.** It was issued on Monday 2026-09-14 and is re-issued here unchanged. Re-issue count: 1.

## Yesterday's result

**Verdict: skipped.**

The evidence, plainly: `D:\Code\Daily-Drill\Daily\Solutions\drill-2026-09-14.py` does not exist. The Solutions folder contains exactly two files, `drill-2026-09-09.py` and `drill-2026-09-10.py`, and nothing from the 14th. The Log section in `drill-2026-09-14.md` is the untouched template — Minutes blank, "AI used: yes / no" still carrying both options, block blank, change blank. The drill file's last-modified time is the moment the task wrote it; you did not open it and save it afterwards. There was no attempt to grade and no report of an attempt, so there were 0 cases run and 0 cases passed.

Streak stays at 0. Levels are unchanged: coding L2, logic L1. No penalty beyond that — a skip costs the streak, nothing more.

**No solution is published today.** The problem below is the same problem, re-issued unchanged, so the solution would destroy it. It arrives the day after you submit an attempt — or the day after this problem is retired, whichever comes first.

One thing worth naming, since it is the only signal available. Two days ago you were told the 52-minute overrun was the real failure and that the fix was to stop at 45 and log where you were. The next thing that happened was a day with no file at all. Those are the same failure wearing different clothes: both are a refusal to submit something unfinished. An attempt that stops mid-way and says "I got to here and could not see past it" is worth more to this process than either a 52-minute green result or a blank folder, because it is the only input that tells you where the ceiling actually sits. A missing file grades as a skip; a broken half-attempt grades as `not_solved` and still gets a full solution written for it the next morning. The blank folder is the one option that returns nothing.

If the block yesterday was the problem itself rather than time — if you sat down, could not see how to start, and closed the editor — that is the case the Log exists for. Write it in the Log and the next run addresses it directly. Leave it blank and it cannot.

## Today's problem

A bakery counts the loaves left on the shelf at the end of every hour and writes each count into a list, in time order.

You are given that list. Consider every pair of hours where one hour comes strictly before the other, and take the earlier count minus the later count. Return the largest such difference. If no pair gives a positive difference — the shelf never holds fewer loaves at a later hour than at some earlier hour — return `0`.

Write `def solve(counts: list[int]) -> int`.

Constraints:
- `0 <= len(counts) <= 100000`
- `0 <= counts[i] <= 10**9`
- The list must not be modified.

Example 1
Input: `[8, 3, 9, 4]`
Output: `5`

Example 2
Input: `[1, 5, 2, 10, 6]`
Output: `4`

## Rules
- Paper first, editor closed. Sketch by hand before typing.
- No AI, no solution tab, no searching the problem.
- 45 minutes maximum. Stuck at 45 means stop and log it.
- Save your attempt as `Solutions\drill-2026-09-15.py` using the exact function name above. No file means no grade.

## Log
- Minutes:
- AI used: yes / no
- Where the block was:
- One thing to do differently:
